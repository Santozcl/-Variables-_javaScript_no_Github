const prompt = require ('prompt-sync')()
const peso = prompt (' qual seu peso?');
const nome = prompt ('Qual seu nome? ');
const altura = prompt  ('qual sua altura? ');
const Internação = prompt ('voce é diabetico? ');
